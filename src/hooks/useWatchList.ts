// create hoooks for watchlist add it to local storage
const useWatchList = () => {
  return {
    addToWatchList: () => {
      // add to local storage
    },
    removeFromWatchList: () => {
      // remove from local storage
    },
  }
}

export default useWatchList
